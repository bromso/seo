Run Biome to lint and auto-fix all files in the monorepo.

## Instructions

1. Run `bun run lint:fix` to auto-fix lint issues
2. Run `bun run format` to format all files with Biome
3. Report any remaining issues that couldn't be auto-fixed

## Expected Output
- Number of files checked
- Number of issues fixed automatically
- Any remaining issues that need manual attention
