# Local k3d development values
global:
  imageRegistry: ""
  environment: development
  domain: localhost

namespace:
  name: seo-audit-dev

ingress:
  enabled: true
  className: traefik
  hosts:
    - host: localhost
      paths:
        - path: /
          pathType: Prefix
          service: www
          port: 3000

# Single replica for local dev with minimal resources
app:
  replicaCount: 1
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi

www:
  replicaCount: 1
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi
