"use client"

// Stub — full implementation in T14 (runAuditAction + RunAuditButton)
export function RunAuditButton({ siteId: _siteId, url: _url }: { siteId: string; url: string }) {
  return (
    <button type="button" disabled className="opacity-50">
      Run new audit
    </button>
  )
}
